# Checks if value is matched in vector

Checks if value is matched in vector

## Usage

``` r
check_match(name = NULL, value = NULL, vec = NULL)
```

## Arguments

- name:

  Argument name.

- value:

  Value.

- vec:

  Vector.
